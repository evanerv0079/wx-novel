# application.yaml
# Server settings (ServerProperties)
server:
  port: 8090
  address: 127.0.0.1
  sessionTimeout: 30
  contextPath: /
 
spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/wxnovel?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=false
    username: root
    password: root